**Super Mega README — Diagnosis Microservice**

... (omitted here: full instructions follow same format as billing)

**Highlights:**
- Consume `exam.completed`, produce `diagnosis.completed` and `diagnosis.requested` as needed.
- Provide `/health` endpoint and durable queue binding to `hospital` exchange.

See `../billing/README.md` for a concrete checklist and examples.
