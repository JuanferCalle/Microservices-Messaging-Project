"""
discharge placeholder

This implementation was removed as part of workspace cleanup. If you need to reimplement
the `discharge` service, create a FastAPI app that connects to RabbitMQ and processes
messages bound to `CONSUME_KEY`.
"""

print("discharge placeholder - implementation removed")
